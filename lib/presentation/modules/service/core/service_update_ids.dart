/// Update IDs for main service screens
enum ServiceUpdateIds {
  currentService('currentService'),
  uiType('uiType'),
  categories('categories'),
  dynamicSections('dynamicSections'),
  background('background');

  const ServiceUpdateIds(this.id);
  final String id;
}
